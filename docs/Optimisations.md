# Optimisations

[Index](Index.md)

Improvement tracker. Categories: `OPT` (performance), `FAC` (DRY), `EXT` (config), `SIM` (lightweight).

- `FAC` — If the prototype grows beyond a single class, extract the data model (`RulesConfig`) and the loading logic into separate files.
